/* const { Router } = require('express');
const router = Router();

const { create, allUsers } = require('../controllers/users');

router.post('/', create);
router.get('/', allUsers);

module.exports = router;
 */
